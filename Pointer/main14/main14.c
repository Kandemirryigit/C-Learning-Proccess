#include <stdio.h>

int main()
{
    int *pvalue=NULL; 
    
    int *pvalue=0;

    return 0;
}


// NULL is a special symbol in C that represents the pointer equivalent to 0 with ordinary numbers
// so *pvalue=NULL and *pvalue=0 is same